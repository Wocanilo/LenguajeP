PROGRAMA
VARIABLES
    x,e:NUM;
    f:SEQ(NUM);
SUBPROGRAMAS
    FUNCION test(NUM a) dev (NUM b)
        VARIABLES
            c:NUM;
        INSTRUCCIONES
            dev 12;
    FFUNCION
    INSTRUCCIONES
    x = 42;
    e = 12;
    mostrar(1+1);
    si(!x==3) entonces
        x = 1;
        mostrar(1-(-2));
    fsi
    x = 122, 122;
    mostrar(x);
