PROGRAMA
VARIABLES
    x,e:NUM;
    f:SEQ(NUM);
SUBPROGRAMAS
    FUNCION test(NUM a) dev (NUM b)
        VARIABLES
            c:NUM;
        INSTRUCCIONES
            a = 12;
            a,b = 1,a+1;
            si(1==1) entonces
                dev 1;
            fsi
            dev 12;
    FFUNCION
    INSTRUCCIONES
    x = 42;
    e = 12;
    mostrar(1+1);
    si(!x==3) entonces
        x = 1;
        mostrar(1-(-2));
    fsi
